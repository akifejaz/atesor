# Runtime dependencies bundled into the .deb virtualenv.
# Dev-only tools (pytest, black, flake8) are intentionally excluded.
#
# Upper bounds pin the tested major versions for reproducible release
# builds (the app currently targets the langchain/langgraph 1.x line).

langchain>=1.0,<2.0
langchain-core>=1.0,<2.0
langchain-openai>=1.0,<2.0
langchain-google-genai>=4.0,<5.0
langgraph>=1.0,<2.0

openai>=2.0,<3.0
google-generativeai>=0.8,<0.9

python-dotenv>=1.0,<2.0
pydantic>=2.0,<3.0
toml>=0.10.2,<1.0
filelock>=3.12,<4.0
requests>=2.31,<3.0
aiohttp>=3.9,<4.0
docker>=7.0,<8.0
termcolor>=2.4,<4.0
